# Dependencias para ejecutar el sistema en local (PC del autor, sin GPU).
# Instalar con: pip install -r requirements.txt

fastapi>=0.110
pydantic>=2.5
uvicorn>=0.27
scikit-learn>=1.4
joblib>=1.3
rapidfuzz>=3.6

# Solo necesarios para el ajuste fino local del codificador preentrenado
# (BETO / RoBERTa-base-BNE) descrito en el apartado 3.6 de la Memoria,
# que queda fuera del alcance entregado en esta fase (ver Cap. 4). No hacen
# falta para lo que ya funciona hoy (generador de datos, base de datos,
# clasificador TF-IDF, API).
# torch>=2.2
# transformers>=4.38
# datasets>=2.18
# seqeval>=1.2
