# Dependencias para ejecutar el sistema en local (PC básico, sin GPU).
# Instalar con: pip install -r requirements.txt

fastapi>=0.110
pydantic>=2.5
uvicorn>=0.27
scikit-learn>=1.4
joblib>=1.3
rapidfuzz>=3.6
